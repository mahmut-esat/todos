import {StyleSheet} from 'react-native';

export default StyleSheet.create({

    container: {
        flexDirection: 'row',
        marginBottom: 10,
        justifyContent: 'center',
      },
      title: {
        fontWeight: '400',
        color: '#d1c4e9',
        marginLeft: 30,
        fontSize: 17,
      },
});
